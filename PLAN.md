# 계획 (앞으로 할 일과 판단 기준)

역할: 앞으로의 계획, 통과 기준, 안전 규칙만 적는 문서입니다. 실험 결과와 발견은 `ExperimentNote.md`, 남은 일과 사용자 몫은 `TODO.md`, 자동 기록은 `docs/results/results.jsonl`입니다.
마지막 갱신: 2026-09-20.

## 앞으로 검토할 아이디어 (2026-09-22 브레인스토밍, 재검토 완료)
AlphaZero 부분 적용(A-F) 작업과 별개로 나중에 고려할 개선 후보들. 우선순위 순:
1. [x] **Soft label 정책 손실 켜기** -- 시도함(2026-09-22, 클라우드 대량 데이터): top1 19.0%/MRR 0.357, 기존 one-hot(19.9%/0.364)과 사실상 동일하거나 살짝 낮음. **개선 없음, unproven.**
2. [x] **Texel tuning**: 도구 완성(`tools/tune/texel-tune.js`), 로컬 소량 검증에서 MSE 18.6%↓, 부호 일치율 66.3%→68.0%. **실전 대전 검증 진행 중**(2026-09-23 디스패치, 200게임, 클라우드).
3. [x] **WASM(AssemblyScript) PoC**: 시도함(2026-09-22) -- `positionalScore` 축소판 벤치마크 0.94x~1.43x(오차범위 안). **압도적 이득 없음, 이 규모로는 투자 보류.** `docs/WASM-POC-RESULT.md`.
4. 사이트 규칙 반영 자가대국(45수/데스매치/별 합계) -- 기존 TODO, 무승부 라벨 왜곡 줄일 수 있음.
5. 퍼징(카드 조합 무작위 대량 생성, 크래시/규칙위반 탐지).
6. 하드 네거티브 마이닝(전술 세트 재구축 선행 필요), 커리큘럼 학습, 오프닝 다양화.
7. SPSA 검색 파라미터 자동튜닝, Singular extension, Aspiration window -- 엔진 핵심부 수정이라 회귀 위험 크고 검증 부담 큼.
8. 제품 트랙(엔진 리스크와 분리): 난이도 조절 모드(MCTS 시제품 재활용 가능), 수 추천/해설 UI(`evaluateStateComponents` 분해값 이미 있음), LLM 해설 레이어.
재검토 후 순위 내림: 오프닝 북(카드 드래프트 때문에 오프닝이 안정적이지 않아 효용 낮음), 엔드게임 캐시(카드 때문에 기물 적은 국면이 드묾), 병렬 탐색(브라우저 워커 간 상태 공유 비용 커서 투자 대비 효과 낮음).

## 2026-09-22 저녁: 새 방안 착수 계획 (병렬)
1. **B 재검증(표본 확대)**: B3의 15개 표본이 노이즈인지 확인 -- 100개+ 로컬 재검증 (가벼움, 로컬 백그라운드)
2. **B soft label 재시도**: `policy-train.yml`에 `--soft`/`--tau` 입력 추가 후 클라우드 재학습 (클라우드, 병렬 가능)
3. **Texel tuning**: `evaluateState` 가중치 자동 튜닝 도구 신규 작성 (서브에이전트, 로컬 코딩만이라 가벼움)
4. **WASM PoC**: AssemblyScript로 `evaluateState` 일부를 컴파일해 속도 벤치마크만 먼저 확인 (서브에이전트, 전체 포팅 아님 -- 가능성 검증 먼저)
5. 사이트 규칙 반영 자가대국은 위 네 개 이후 (우선순위 낮음, 보류)
서브에이전트 2개(Texel, WASM PoC) + 로컬 백그라운드 1개(B3 재검증) 동시 진행. 로컬 자원 한도(4/8코어) 준수.

## 목표
같은 시간에 더 강하고, 사이트 규칙과 정확히 일치하는 엔진. 강해졌다는 것은 대전 측정으로만 인정한다.

## 진단 (근거는 ExperimentNote.md)
1. 검색이 느리다: 시간의 절반 이상이 트리 탐색이 아니라 루트 안전 검사에 쓰인다 (2.2배 개선 후에도 깊이 4는 수십 초).
2. NNUE 라벨이 대부분 무승부라 수제 평가를 넘기 어렵다 (깊이별 가중 라벨의 첫 win은 독립 재확인에서 재현되지 않음 -> unproven).
3. 측정이 약하다: 결정된 게임이 적어서 큰 차이만 보인다.
의존 순서: 측정(C) -> 속도(A) -> 평가(B) -> 다시 속도.

## C. 측정
- [x] 신뢰구간과 구분 가능한 차이 표시, `verdict.json` 자동 판정 (`match.yml`)
- [x] 핸디캡(`handicap`), 독립 재실행(`seed_offset`), 검색 파라미터(`params_a/b`) 옵션
- [x] 실험실 실행 도구 `tools/lab/lab.js` (dispatch/collect/status), 결과 로그 `docs/results/results.jsonl`
- [ ] 무승부 원인 확인: 핸디캡을 올려도 무승부가 39~45%에서 안 줄어듦 -> 자가대국의 반복/50수 무승부 규칙 점검
- [ ] 고정 오프닝 세트, 조기 종료(순차 검정), 게임 수 400 이상
- [ ] 전술 세트 재구축(기준 검색 깊이 4 이상, 300문제 이상), 수 품질 점수

## A. 속도 (통과 기준: 고른 수, 점수, 노드, 컷오프가 동일하고 시간이 줄어야 함)
- [x] 루트 즉시 패배 검사 필터, 위험 캐시, 말 순회 최적화, 조용한 수 탐색 정렬 최적화 (누계 약 2.2배)
- [x] CPU 프로파일 실측(2026-09-23, `--cpu-prof`, 실전 8위치): `cloneState` 13.2%, **`attacksSquare`+`attacksSquareRaw` 14.4%(1위)**, GC 5.8%. `attacksSquare`는 kingSafetyScore/specialThreatScore/hangingMaterialRisk가 전부 의존 -- 캐싱/메모 확장 여지 있음, 다음 순서로 착수 후보
- [ ] `attacksSquare` 캐싱 확장 (실측 1위 병목, 새로 확인)
- [ ] `cloneState` 공유(변경되지 않는 필드), 나머지 루트 안전 검사 필터 (위반 0건이 1만 건 이상일 때만)
- [ ] 위험 증분 계산 (다른 항목이 끝나도 비용이 클 때만, 그림자 모드로 검증)
- [ ] 검색 파라미터 튜닝(널무브, LMR, 조용한 수 탐색 깊이)은 실험실에서 후보로 대전

## B. 평가
- [x] 잔차 학습(`RESIDUAL=1`) 구현 -> 결과는 unproven
- [x] 3라운드 전체(150,415) 재학습 여러 변형 (`ExperimentNote.md` 2번, 6번)
- [x] 깊이별 가중 라벨(`blenddepth`)은 첫 win 후 독립 재확인 2회에서 재현 안 됨(합산 56.2%, 구간 49.0~63.2%) -> unproven, 확장에는 실험 모델로만 노출
- [ ] 라운드 4: 속도가 오른 엔진으로 더 깊은 라벨. 라벨/블렌드 변형만으로는 수제 평가를 못 넘으므로, 더 깊은 검색 라벨(깊이 4 이상)과 데이터 다양성이 열쇠
- [ ] 수제 평가 보정은 승패 기반으로만 (검색 점수 회귀는 단위가 달라 폐기)

## 실험 공장 (실험실 위에 쌓는 순서)
- 실험실(판정기): 완료 (위 C 참조)
- 후보 생성기: 재학습 변형, 검색 파라미터, 깊이/시간 프리셋
- 자동 루프: 자가대국(최고 모델) -> 데이터 -> 학습 -> 실험실 판정 -> win이면 승격. 아직 미구현
- 감시: 사이트 패치 감시 워크플로 `site-watch.yml` 추가됨 (첫 실행 미확인)

## 통과 기준
- 속도/무회귀 변경: 결과 동일 + 시간 감소. 검증은 컴퓨터가 한가할 때 (부하가 시간 기반 검사를 흔든다).
- 평가/출시: 95% 구간 하한 > 50%이고 독립 재실행에서도 같은 방향. 후보가 여럿이면 우연 통과 확률(4개 중 약 19%)이 있어 반드시 재확인.
- 통과 못 하면 unproven으로 기록만 하고 반영하지 않는다.

## 자율 진행 규칙
- 작은/중간 위험 결정은 직접 하고 보고, 큰 위험은 다른 일을 진행하며 보류.
- 마지막 단계: 코드 리팩터링, 삭제, 푸시, 그다음 최종 보고 (수확, 잡은 위협, 버그, 기타).
- 기본값: 패럿 안전장치는 넣지 않음, 잔차/신규 모델은 선택 모델로만 추가(기본값 변경 금지), 스냅샷 삭제는 사용자 몫.
- 서브에이전트/병렬은 얻는 것이 확실할 때만.

## 위험과 대비
- 6시간 주기 자가대국이 최신 엔진을 받으므로, 엔진 변경은 동일성 검증 후에만 푸시한다.
- 클라우드 동시 작업 20개 한도(자가대국 8개 사용 시 다른 작업 12개 이하), 데이터 브랜치와 아티팩트 용량을 지켜본다.
- 확장 엔진 사본 어긋남은 CI(`tools/ci/engine-sync.js`)가 검사, 엔진을 고치면 사본을 맞춘다.
- 조용한 실패: 학습/대전 로그를 매번 읽는다 (모델 저장 버그를 로그에서 찾았다).
- 승인이 필요한 삭제는 마지막에 따로. 자동 모드가 막으면 명령을 사용자에게 남긴다.

## AlphaZero 아이디어의 부분 적용 (2026-09-20) -- 큰 계획에서 작은 계획으로

### L0 목표
사이트 AI를 따라 하는 알파-베타 엔진(Twist)에 알파제로의 아이디어 중 이득이 확인되는 것만 옵션으로 넣고, 러스트/파이썬 팀에는 "무엇이 통하고 안 통하는지" 근거를 넘긴다. 사이트 일치(규칙, 안전 검사)는 깨지 않는다.

### L1 트랙
- A 데이터: 검색이 고른 수와 상위 후보 점수를 기록한다.
- B 수 순서 정책: 수마다 점수를 매기는 작은 모델로 정렬을 개선한다(고정 크기 정책 출력은 쓰지 않음).
- C MCTS 시제품: 수제 평가를 값, 수 정렬 점수를 사전 확률로 쓰는 별도 탐색 모드. 실험으로만.
- D 자가대국 다양성: 루트 후보 점수 소프트맥스 샘플링.
- E 재분석: 옛 국면을 더 좋은 엔진으로 다시 계산 (러스트 이후가 훨씬 싸므로 보류).
- F 이식성: 데이터는 브리지 상태 JSON과 호환, 모델은 JSON 가중치 + 순수 함수.

### L2 단계와 통과 기준
- A1 [완료] `selfplay-worker-merged.js`: `recordPolicy`(환경변수 `SELFPLAY_RECORD_POLICY=1`, 기본 꺼짐)로 `policy: {chosen, n, top:[{a,s}]}` 기록. 추가 필드만 붙이고 난수 호출과 착수 결정은 건드리지 않음. 확인: 한 판에서 chosen == 1위 후보 21/21, 기록당 약 1KB.
- A2 깊이 3 이상 자가대국으로 약 2만 국면 수집(클라우드 8샤드, 반나절). 기존 R3의 깊이 분포가 얕아서(깊이 3이 50%, 깊이 2가 24%, 깊이 5 이상 5%) 깊이 3 이상만 씀.
- B1 오프라인 지표: 현재 정렬(`actionOrderingScore`+킬러)의 1위/상위 3위 적중률과 같은 깊이에 필요한 노드 수. 기준선이 된다.
- B2 수 점수 모델 학습(선형 또는 작은 MLP, 가중치 JSON). 검증 정답은 "깊은 재검색(깊이 5, 2천 국면)의 선택".
- B3 옵션 통합 `options.params.policyOrdering`(기본 끔). 통과 기준: 같은 깊이 노드 수 감소 + 실험실 판정이 lose가 아님(정렬이 LMR/널무브에 걸려 결과를 바꿀 수 있으므로 동일성이 아니라 판정으로).
- C1 MCTS 시제품(별도 모듈, 엔진 본체 불변): PUCT, 값 = tanh(수제 평가/400), 사전 확률 = 정렬 점수 소프트맥스, 루트에서 즉시 승패 검사 재사용, 확률적 수는 한 번 뽑은 결과로 고정.
- C2 같은 시간의 알파-베타와 실험실 대전(핸디캡 1, 독립 재확인). 통과 기준은 다른 후보와 동일(구간 하한 > 50%).
- D1 자가대국 루트 소프트맥스 샘플링 옵션 -> 오프라인에서 무승부율과 국면 다양성 비교.
- E, F 러스트가 생긴 뒤. F는 지금도 데이터 형식만 맞춰 둔다.
순서: A1 -> (A2 클라우드) ∥ C1 -> B1 -> C2 -> B2 -> B3 -> D1.

### 얕은 탐색/검색이 일으킬 수 있는 문제와 대비
- 라벨과 정책이 얕은 검색에서 나오면 얕은 판단을 따라 배운다: 깊이 3 이상만 사용, 상위 후보 분포로 학습, 정답은 더 깊은 재검색.
- 자기 실수를 강화하는 순환: 새 정책은 실험실 판정을 통과해야 반영.
- 잘못된 정렬은 정확성이 아니라 속도만 해친다고 생각하기 쉽지만, 시간 제한이 있으면 느려진 만큼 얕아져서 약해진다: 오프라인 노드 수와 판정으로 확인.
- 루트 후보 걸러내기(정책 기반)는 유일한 좋은 수를 버릴 수 있다: 잡기와 결정적 수는 절대 걸러내지 않고, 이 항목은 B3 통과 후 옵션으로만.
- MCTS 200번 시뮬레이션은 1수 반격을 놓칠 수 있다: 루트 안전 검사 재사용.
- 기록되는 후보는 루트 안전 검사를 통과한 것뿐이라(평균 10개 정도) 전체 26개 수에 대한 정책이 아니다: 정렬 대상이 "검색이 고려하는 후보"임을 문서화.

### 두 번째 검토에서 고친 것
1. "정렬만 바꾸면 결과가 같다"는 가정은 틀렸다: LMR과 널무브가 정렬에 의존하므로 동일성 검증이 아니라 실험실 판정으로 통과 여부를 정한다.
2. 데이터 깊이 분포를 확인하고 깊이 3 이상만 쓰기로 했다.
3. 정책 기록 후보가 안전 검사 후의 후보라는 한계를 명시했다.
4. 자가대국은 같은 시드로도 재현되지 않는다(엔진 안에 시드로 통제되지 않는 무작위가 있음). 그래서 "기본 출력이 같은지"는 전체 대국 비교가 아니라 코드 검토와 필드 단위 검사로 확인했다.
5. 자원: A2는 클라우드 8자리 반나절이라 상한(20개) 안. 러스트와 파이썬 팀 작업과 겹치지 않는다(데이터를 우리 저장소에만 둠).

### 정정: 항목 2(루트 후보 걸러내기)도 러스트 포팅을 고려한다 (2026-09-20)
러스트 엔진에서 이 아이디어는 "정책 사전 확률로 후보 수를 제한하는 점진적 확장(progressive widening)"이 된다. 포팅 방법:
1. 러스트 쪽에 루트 안전 검사(즉시 패배 검사, 하드/소프트 안전 검사)를 함수 단위로 옮긴다. 사이트 일치가 필요한 봇이라면 어차피 필요하다.
2. 정답지 생성기가 후보 수마다 안전 검사 결과(통과, 하드 탈락, 소프트 탈락과 이유)를 함께 기록하게 확장한다 -> 러스트가 함수 단위로 같은지 대조한다(오라클이 정답).
3. 정책은 "걸러내기"가 아니라 "검사 순서와 우선순위"로 먼저 쓴다(결과 불변). 그다음 잡기와 결정적 수는 항상 유지하고 상위 K개만 검사하는 옵션을 만든다(결과 변경, 기본 끔).
4. 통과 기준: (a) 안전 검사 함수 대조 불일치 0건, (b) 걸러낸 버전과 안 거른 버전이 고르는 수의 일치율, (c) 실험실 대전 판정.
5. 러스트에 MCTS가 있으면 같은 정책이 사전 확률로 들어가므로 이 항목은 MCTS의 일부로 흡수된다.

### 진행 기록 (AlphaZero 부분 적용, 2026-09-20)
- A1 완료: 정책 기록(`SELFPLAY_RECORD_POLICY`)에 현재 정렬이 검색이 고른 수를 몇 등에 뒀는지(`chosenRank`, `nLegal`, 후보별 정렬 점수 `o`)와 선택적 국면 전체 저장(`SELFPLAY_RECORD_STATE`)을 추가. 기본 꺼짐.
- A2 시작: 클라우드 자가대국 8샤드, 깊이 6/1500ms, 목표 2만 국면, 정책과 국면 기록 켬. 라운드 시작 `20260920T122511Z`(저장소 변수 `SELFPLAY_ROUND_START`). 끝나면 변수를 원래 값(TARGET 150000, RECORD_* 끔)으로 되돌린다.
- B1 도구: `tools/policy/ordering-eval.js`(현재 정렬의 1위/3위 적중률과 평균 순위를 무작위 정렬과 비교). 시험 대국 한 판(9개 기록)에서 검색이 고른 수의 정렬 순위가 7,3,2,10,5,6,3,4,16 (합법 수 27개 중)으로, 1위 적중 0/9, MRR 0.23(무작위 0.14) -> 현재 정렬은 무작위보다 조금 나은 수준일 가능성. 표본이 작아서 클라우드 데이터로 확인한다.
- C1 완료(서브에이전트): `tools/mcts/mcts.js`(PUCT, 값 = 수제 평가, 사전 확률 = 정렬 점수 소프트맥스, 루트 즉시 패배 검사 재사용), `searchFnByColor` 옵션, 대전 사양 `mcts:<sims>`. 측정: 초당 시뮬레이션 약 7회(부하 있는 8코어), 1회당 약 75ms(수 생성 22, 복제와 적용 18, 평가 20, 정렬 15). 앞서 낸 추정(초당 80회)은 복제, 적용, 평가만 더한 값이라 수 생성과 정렬 비용을 빠뜨렸다. 자바스크립트에서는 100번 시뮬레이션이 4~24초 -> 같은 시간 대전은 알파-베타가 유리한 조건이라 결과 해석에 주의. 실질적인 MCTS 평가는 러스트가 생긴 뒤가 맞다.
- 교훈: 로컬 자가대국은 스레드 7개로 컴퓨터를 멈추게 했다 -> `SELFPLAY_WORKERS` 상한 추가, 무거운 작업은 클라우드로.

### 진행 기록 (2026-09-22)
- A2: 클라우드 16샤드 실행이 cutoff(2026-09-22T08:00:00Z)에 도달, 종료 확인 중. 종료되면 저장소 변수를 기본값(SHARDS=[1..8], TARGET=150000, RECORD_POLICY=0, RECORD_STATE=0)으로 되돌리고 `dataset-build.yml`로 데이터셋을 만든다.
- B2 시작: `tools/policy/{common,features,score-actions,train-policy}.js`(이전 세션에서 작성, 미검증 상태)를 로컬 소량 자가대국(깊이 4, 2코어, 정책+상태 기록 켬)으로 실제 동작 검증 중 -- 아직 데이터 없음, 재시도 중.
- 큰 그림 재정리(비유 포함, 사용자에게 설명함): L0 목표(검증된 것만 옵션으로) -> L1 다섯 트랙(A 기록, B 정책, C MCTS(보류), D 다양성, F 이식성) -> L2 각 트랙의 작은 단계. 우선순위는 그대로 A -> B -> (C 보류) -> D -> F.
- 러스트 곁가지 조사 및 결론: 팀원 저장소(`sungjeahyun100/augment-chess-bot`, `docs/RUST-PORT-REFERENCE.md`)가 이미 Phase 3(변형 기물)까지 진행돼 있어 "우리가 쓰는 기물만 포팅"을 검토했으나(37종 중 11종 미구현), 평가함수(`evaluateState`)만 따로 떼어 포팅하는 것도 확인했다 -> **평가함수는 `isSquareAttacked`(전체 기물 공격 판정)와 카드 상태(`delayedHazards`)에 강하게 얽혀 있어 독립 포팅 불가**. 즉 C(MCTS) 보류 사유(러스트 필요)를 우회할 방법이 없다는 것만 확인하고, 러스트 작업 자체는 지금 하지 않는다. A-F는 전부 JS 안에서 할 수 있는 만큼 진행한다.
- 2026-09-22 사용자 지시: L1 다섯 트랙 전부(A-F, 가능한 범위 내) 진행. TODO.md에 순서대로 체크리스트를 만들고 하나씩 끝낼 때마다 중단 없이 보고한다.
