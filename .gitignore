node_modules/
selfplay-data*.jsonl
selfplay-data.jsonl.bak-*
nnue/cache/
nnue/snapshots/
# Large intermediate merges of self-play checkpoints (regenerable from the
# gha-segments-16cards branch + local selfplay-data* files) -- not meant to
# be committed, easily exceeds GitHub's 100MB single-blob limit.
nnue/aggregated/
*.zip
scratch_*.json
scratch_*.jsonl
*.pid
.claude/scheduled_tasks.lock
