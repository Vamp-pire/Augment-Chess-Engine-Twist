# Changelog

이 저장소의 눈에 띄는 변경을 날짜순으로 기록합니다(최신이 위).

## 2026-09-20

### 엔진
- 검색 약 2.2배 빨라짐(고른 수, 점수, 노드, 컷오프 동일 검증): 루트 즉시 패배 검사 필터, 위험 캐시와 공격 메모, 말 순회 최적화, 조용한 수 탐색 정렬 최적화
- 실험용 검색 파라미터(`options.params`: 널무브, LMR, 조용한 수 탐색 깊이), 기본값 동작은 동일

### 학습/대전
- 잔차 학습 옵션(`RESIDUAL=1`), 전술 테스트 도구(`nnue/tactics.js`), 대전 핸디캡/시드 오프셋/파라미터 옵션, 신뢰구간과 구분 가능한 차이 표시, `verdict.json` 자동 판정
- 실험실 실행 도구(`tools/lab/lab.js`)와 결과 기록(`docs/results/`)

### 인프라
- CI: 확장 엔진 사본 동기화 검사(`tools/ci/engine-sync.js`), 사이트 패치 감시 워크플로(`site-watch.yml`)
- 모델 저장 단계의 브랜치 충돌 버그 수정, 문서 재정리(`PLAN.md`, `TODO.md`, `ExperimentNote.md`)

## 2026-09-19

### 엔진
- 검색 제한을 Stockfish처럼 조정 가능하게: `searchBestAction(..., { limits })`로 최대 깊이, 생각 시간, 최소 깊이, 무제한(infinite), 노드 수를 지정. 시간이 다 돼도 현재 깊이를 60% 이상 봤거나 최선수가 바뀌는 중이면 최대 2배까지 연장하고, 끝낼 수 없는 다음 깊이는 시작하지 않음. `limits`를 주지 않으면 기존 동작과 동일(60개 판 대조, 차이 0). 자기대국은 영향 없음
- 사이트 9/19 패치(밸런스, 카드 리메이크, 판정 정리)를 `engine-merged.js`에 반영. 사이트 AI 워커와 수 목록이 무작위 판 600개에서 모두 일치
- 사이트 규칙과의 차이 수정: 포탈건 이동 차단 해제, 도둑의 두 번째 이동, 6개 수정 게이트, 카드 효과 뒤 캐시 무효화, 교환 이동 뒤 도둑 이동 기록
- 평가 함수 약 3.4배, 검색 약 1.5배 빨라짐(출력은 원본과 동일함을 검증)

### 확장
- "세부 설정"에 우리 엔진 검색 제한(최대 깊이, 생각 시간, 최소 깊이, 시간 연장) 추가. 리뷰와 실전 봇에 적용(봇은 사이트 제한 시간의 1.5배까지만 연장)
- 확장이 학습에 쓴 것과 같은 엔진과 NNUE 인코더(기물 40종, 카드 184장) 사용
- NNUE 모델 선택(Squall 기본 / Tornado), 불러오기 상태 표시, 성능 패널에 사용한 평가기 표시
- 엔진 교체로 사라졌던 검색 진행 표시와 성능 패널 복구
- 분석 결과 아이콘을 직접 만든 것으로 교체(이전 아이콘은 재배포 조건이 맞지 않는 출처였음)

### 학습과 자기대국
- 검색 점수 비중, 모델 크기, 워밍스타트 실험(검증 정확도 67.1% → 최대 71.1%). 실전 대결에서는 Squall보다 낫지 않았음
- 3라운드 자기대국 시작(깊이 6, 병렬 8개, 목표 15만 개, 스스로 종료)

### 개발 환경
- 워크플로 정리: `ci.yml`, `selfplay.yml`, `nnue-train.yml`, `match.yml`, `dataset-build.yml`
- CI 검사 추가(문법, 스모크, 평가값 회귀, 확장 == 학습 점수, 사이트 대조)
- 폴더 정리(`legacy/`, `data/`), `PROJECT.md`, `README.md`, `LICENSE`, `NOTICE.md`
