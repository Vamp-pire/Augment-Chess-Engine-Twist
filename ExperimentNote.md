# ExperimentNote (실험 세팅과 비율 기록)

마지막 갱신: 2026-09-24. 결과 원본은 `docs/results/results.jsonl`, 계획은 `PLAN.md`.

## 1. 데이터

| 이름 | 국면 수 | 수집 설정 | 비고 |
|---|---|---|---|
| Round 1 | 218,007 | 깊이 3 이하, 90%가 깊이 1 | 옛 규칙 |
| Round 2 | 112,981 | 깊이 5(명목), 91%가 깊이 1 | Squall 학습 데이터 |
| Round 3 (`round3-full`) | 150,415 (목표 150,000) | 깊이 6, 수당 1500ms, 8샤드, 시작 2026-09-19T03:53Z | 깊이 2~6 라벨, `since=20260919T035300Z` |
| Round 4 (`round4-fullstate`) | 45,438 | 깊이 6, 수당 1500ms, 8샤드, 시작 2026-09-23T12:10Z, 컷오프 20:00Z | `compactBoard()`가 기물 상태 70개+ 필드를 전부 보존하도록 고친 뒤(2026-09-23) 첫 데이터. 3개 샤드는 GitHub 자체 job 타임아웃으로 마지막 체크포인트 유실(알려진 동작, `selfplay.yml` 주석 참고) |
| 검증 세트 `val-mix1` | - | R2 꼬리 + R3 꼬리 | R3 일부가 학습과 겹칠 수 있음(정확도는 참고용) |

- 자가대국 변수(저장소 변수): `SELFPLAY_SHARDS`(8), `SELFPLAY_DEPTH`(6), `SELFPLAY_MS`(1500), `SELFPLAY_TARGET`(150000), `SELFPLAY_ROUND_START`, `SELFPLAY_CUTOFF_ISO`(백스톱 2026-09-22T04:00Z).

## 2. 학습 세팅 (`nnue/train.js`, `nnue-train.yml`의 overrides)

| 항목 | 값 / 의미 |
|---|---|
| 입력 | 5509(기본) = 말 평면 40x128 + 카드 184x2 + 특징 21. `FULL_PIECE_STATE=1`이면 15237(기물당 HP/보호막/빙결 등 76개 속성 x 2 x 64칸 추가) -- 기본 꺼짐, 확장(`extension/nnue.js`)과 기존 배포 모델이 전부 5509라서 켜면 즉시 어긋남(2026-09-23 CI가 실측으로 잡음, `nnue/encode.js` 참고) |
| 구조 | wide & deep, deep 유닛 기본 16 (`ablateDeepUnits`, 8도 시험) |
| 라벨 | `(1-w) * 승패 + w * tanh(검색점수/400)` |
| 기본 검색 비중 w (`ablateBlend`) | 0.15 (실험: 0.3~1.0) |
| 깊이별 비중 (`blendByDepth=1`) | 깊이 1,2,3,>=4 -> 0.25, 0.45, 0.65, 0.8 (`blendDepthMap`) |
| 잔차 (`residual=1`) | 라벨 = tanh((검색점수 - evalBefore) / `residScale`); 검색 점수 없는 국면은 0 |
| 잔차 계수 | 150 / 300 / 600 (사용 시 `@hybrid<계수>`) |
| 종료 시점 가중 | 게임 끝까지 남은 수 `pliesFromEnd` 기준 0.98^n 감쇠 |
| 웜스타트 스케일 | 0.001 (`ablateWarmStart`) |
| 오버샘플 | 1.5 (`ablateOversample`) |
| 저장 | `save_model=<이름>` -> 데이터 브랜치 `models/<이름>.json` |

## 3. 출력 매핑 (대전 사양 `<모델>@<맵>`)

- `atanh<K>`: K * atanh(출력), 학습 압축 tanh(점수/400)의 역, 보통 `atanh400`
- `hybrid<K>`: 수제 평가 + K * 출력 (잔차 모델용)
- `lin<K>`: K * 출력, 기본(맵 없음)은 x100(`SCORE_SCALE`)

## 4. 대전 세팅 (`match.yml`, `tools/lab/lab.js`)

- 색 교환 짝(같은 시드에서 색만 바꿈), 4샤드, 총 게임 = 4 x pairs_per_shard x 2
- 기본: `ms=300`, `depth=3`; 실험실 기본 후보 대전은 `pairs_per_shard=12`, `handicap=1`
- `handicap=N`: 시드로 고른 한쪽에서 비퀸 말 N개 제거(짝은 같은 쪽이 손해라 공정). 무승부: 핸디캡 0 약 57%, 1 약 41~45%, 2 약 39%
- `seed_offset`: 짝 번호에 더해 독립 재실행. `params_a/b`: 검색 파라미터 (`nullMoveMinDepth`=3, `nullMoveReduction`=2, `lmrMinDepth`=3, `lmrMoveThreshold`=4, `quiescenceMaxPlies`=6이 기본)
- 판정(`verdict.json`): win = 95% 구간(Wilson) 하한 > 50%, lose = 상한 < 50%, 그 외 unproven
- 구분 가능한 차이(80% 검정력): 결정 45판 약 +-21p, 결정 100판 약 +-14p, +-5p에는 약 784판

## 5. 엔진 시간/깊이 세팅

- 확장 "우리 엔진": `augEngineOwnMaxDepth`, `ThinkTimeMs`, `MinDepth`, `Extend`(시간 연장)
- 시간 연장 옵션(`limits`): softMs, hardMs(상한 1.5x), extendOn, minDepth, extendMinProgress, predictiveStop, maxNodes
- 속도 개선 누계: 이전 엔진 대비 약 2.2배 (카드 국면 포함, 노드/컷오프 동일)

## 6. 결과 요약 (수제 평가 상대, 후보 = A)

| 후보 | 승률(결정판) | 95% 구간 | 판정 |
|---|---|---|---|
| resid150-r3f @hybrid150 | 44.4% (54) | 32.0-57.6 | unproven |
| resid300-r3f @hybrid300 | 53.1% (49) | 39.4-66.3 | unproven |
| resid600-r3f @hybrid600 | 45.6% (57) | 33.4-58.4 | unproven |
| blenddepth-r3f @atanh400 (첫 실험) | 66.0% (53) | 52.6-77.3 | win |
| blenddepth-r3f @atanh400 (재확인, 핸디캡 0, seed_offset 1000) | 50.8% (65) | 38.9-62.5 | unproven |
| blenddepth-r3f @atanh400 (재확인, 핸디캡 1, seed_offset 1000) | 53.7% (67) | 41.9-65.1 | unproven |
| resid300-du8-r3f @hybrid300 | 44.0% (50) | 31.2-57.7 | unproven |
| blend08-r3f @atanh400 | 51.0% (51) | 37.7-64.1 | unproven |
| blenddepth-du8-r3f @atanh400 | 45.7% (46) | 32.2-59.8 | unproven |

- 그 외 참고: 시간 연장 40승 31패(56%, 합계), 깊이 3 대 2 약 57%(61판), 옛 Squall 대비 등은 모두 오차 범위 안.
- **blenddepth 재확인 결과**: 독립 재실행 두 번이 50.8%, 53.7%(합쳐서 52.3%, 구간 43.8~60.6%)로 첫 66%가 재현되지 않았다. 세 번을 모두 합치면 56.2%(104/185, 구간 49.0~63.2%)로 아직 unproven. 첫 결과는 후보 여러 개 중 가장 좋은 것을 골랐기 때문에 나온 과대평가(우연)일 가능성이 크다. 결론: 수제 평가를 확실히 넘는 모델은 아직 없음.
- **Texel-tuned vs handcoded** (2026-09-23, `tuned:tools/tune/weights.json`, 80게임): 26승18패33무, tuned 기준 59.1%, 구간 44.4~72.3% -> **unproven** (0.5 포함).

## 7. `fullstate-v1` (Round 4, 15237차원, 2026-09-23)

- `FULL_PIECE_STATE=1`로 학습, 45,438 포지션(38,086 학습/검증 분할), epoch 8에서 조기 종료(val_loss 0.4356).
- 결정적 검증 포지션 부호 일치 정확도: **78.5%** (1660/2115).
- 데이터 브랜치 `models/fullstate-v1.json`에만 저장, **master에 승격 안 함** -- 다른 모델과 서로 다른 데이터로 학습해서 이 정확도 숫자만으로 직접 비교 불가, 실전 대전(`match.yml`)으로 검증 필요.
- 확장(`extension/nnue.js`)은 여전히 5509차원 -- 이 모델을 실제로 쓰려면 확장 인코더 포팅 + 배포 전환이 별도로 필요.

## 8. NNUE-eval 자가대국 피드백 루프 실험 (2026-09-24)

`selfplay-worker-merged.js`의 `SELFPLAY_NNUE_EVAL` 옵션(자가대국이 수제 평가 대신 모델 자신의 평가로 두게 함)은 "자기강화/피드백 루프 위험"으로 2026-09-12경 기본 꺼짐 처리됐으나, 그 논의 자체가 문서화되지 않아 근거가 코드 주석에만 남아있었음 -- 이번에 직접 소규모 검증.

**설계**: 클라우드 19샤드, 짧게(4분씩), 홀수=handcoded 기준선/짝수=`fullstate-v1` NNUE-eval(+ 완화책으로 소프트맥스 탐험 샘플링 `SELFPLAY_SAMPLE_TAU=0.3`). `.github/workflows/nnue-eval-experiment.yml`, 데이터는 `experiments/nnue-eval-test/`(메인 데이터셋과 완전 분리, `dataset-build.yml`은 `segments/`만 읽어서 섞일 수 없음).

**결과** (17/19샤드 성공, 2개는 브랜치 push 경합으로 마지막 체크포인트 유실 -- 알려진 사소한 손실, 재시도 횟수를 8회로 늘렸는데도 발생, 더 늘리거나 샤드별 push 간격을 넓히는 개선 여지 있음):

| 조건 | 샤드 | 포지션 | 결정 | 무승부 | 미종료 | 오프닝 다양성(첫 3수 국면 서명) |
|---|---|---|---|---|---|---|
| handcoded | 8 | 7,680 | 55.3% | 40.8% | 3.9% | 7종 |
| nnue-eval | 9 | 8,624 | 45.5% | **52.7%** | 1.7% | 8종 |

**해석**: 오프닝은 탐험 샘플링 덕분에 붕괴하지 않음(7 vs 8종, 비슷). 다만 **무승부율이 눈에 띄게 상승**(40.8%→52.7%) -- 우려했던 "자기 판단을 과신해서 더 소극적/안전 위주로 수렴"하는 패턴과 방향이 일치.

**재확인** (2026-09-24, 같은 조건에서 표본 4배: 4분→15분×19샤드, 17/19샤드 성공, 2개는 이번에도 push 경합으로 유실 -- 같은 재시도 한도로는 완전히 안 없어지는 사소한 손실로 확인):

| 조건 | 샤드 | 포지션 | 결정 | 무승부 | 미종료 |
|---|---|---|---|---|---|
| handcoded | 9 | 28,325 | 55.0% | 40.3% | 4.7% |
| nnue-eval | 8 | 24,765 | 46.9% | **50.2%** | 3.0% |

1차(55.3/40.8/3.9 vs 45.5/52.7/1.7)와 거의 동일한 비율로 **재현됨** -- 노이즈가 아니라 실제 패턴. **결론: 자기강화 위험 신호 확정(confirmed, 단일 세대 기준)** -- 여전히 다세대 드리프트는 미확인이지만, 최소한 "1세대 안에서 이미 무승부 쏠림이 뚜렷하다"는 건 표본 4배로도 재현됐으므로 더 이상 노이즈로 볼 수 없음. 메인 파이프라인에 섞지 않는다는 방침 유지, 실제로 쓰려면 (a) 기준 모델과의 대전 게이트, (b) 무승부 쏠림 자체를 완화할 방법(탐험 강도 상향 등) 먼저 필요.

## 7. 통과 기준 (수정본)

- 출시(확장 기본 모델 등)는 95% 구간 하한이 50% 위이고, 독립 재실행에서도 같은 방향일 때만.
- 속도/무회귀 변경은 결과 동일(고른 수, 점수, 노드, 컷오프)로 판정, 기계가 한가할 때 검증.
- 후보를 여러 개 시험하면 우연 통과 가능성(4개 중 약 19%)이 있으므로 반드시 재확인한다.

## 8. 발견 기록 (PLAN.md/TODO.md에서 옮김)

### 2026-09-19
- 2라운드 검증(결정 3,159국면, 오차 약 +-1p) 정확도: Squall 기준(blend 0.15) 67.1%, 웜스타트 없음 67.2%, blend 0.3/0.5/0.7/0.8/0.9/1.0 = 69.1/69.2/71.1/70.6(3회 69.5~70.8)/69.8/67.8, deep 유닛 4/8/8+blend0.5/8+blend0.8 = 67.4/68.7/68.9/69.5. 새 엔진 특징으로 재인코딩한 blend 0.8은 70.6%로 같아서 9/19 규칙 변경이 재학습을 강제하지 않음.
- 검색 점수 비중을 올리면 정확도는 +2~4p 오르지만 실전에서는 blend 0.8이 Squall보다 낫지 않았음(42게임: 5승 9패 27무 1미완) -> 정확도가 곧 강도는 아님.
- 클라우드 대전(깊이 3, 300ms, 결정된 게임만, 전부 오차 범위): Tornado 대 Squall 13-17, Typhoon 대 Squall 20-15, 수제 평가 대 Squall 19-15. 깊이 2 대 4(수제, 1500ms) 6-8.
- 무승부가 지배적이라 결정된 게임으로만 판단하고, 10판 미만은 신뢰하지 않는다.
- 평가 약 3.4배, 검색 약 1.5배 개선(출력 동일, `tools/perf`).
- 학습된 망은 말 손익에 거의 반응하지 않음(시작 국면에서 퀸 하나 없을 때 Squall 출력 -0.011 대 정상 +0.078, 수제 평가는 -1284). 라벨이 대부분 무승부라 출력이 0 근처에 몰림.
- 검색 점수 -> 승률 보정(`tools/review-calibration`): 깊은 검색일수록 예측력이 높음(k 전체 3450, 깊이 4~5에서 약 2200). 리뷰 임계값은 이미 손실 1/2.5/5/10% 근처라 값은 적절, 부족한 것은 맥락과 깊이 기반 신뢰도.

### 2026-09-20
- **속도**: 누계 약 2.2배(이전 엔진 대비, 카드 국면 포함 16국면에서 고른 수/점수/노드/컷오프 동일). 구성: 루트 즉시 패배 검사 필터(1.5~1.6배), 위험 캐시와 공격 메모(1.16배), 말 순회 최적화(1.08배), 조용한 수 탐색 정렬 최적화. 노드 수가 한 번 다르게 나온 것은 부하 때문에 시간 기반 검사(`rootSafetyDeadlineTight`)가 달라진 것이었고 한가한 상태에서는 12/12, 16/16 동일. 규칙: 동일성 검증은 컴퓨터가 한가할 때.
- **병목**: 깊이 2 검색이 한 수에 4~12초(노드 400개 남짓, 노드당 약 30ms). 시간의 절반 이상이 루트 안전 검사. 엔진에는 이미 전치표, 널무브, LMR, 킬러가 있음.
- **시도했다 버린 것**: `clonePiece` 키 목록 끌어올리기(이득 0%). 검색 점수를 정답으로 정적 평가 가중치를 회귀(검색 점수에 수마다 붙는 보너스가 섞여 단위가 다름, 현재 가중치 R2 -2.3) -> 승패 기반 튜닝만 가능.
- **시간 연장**(수제, 1500ms): 재대전 22-21, 앞선 결과와 합쳐 40-31(56%)로 유의하지 않음 -> 옵션으로 유지, 미확인.
- **깊이 3 대 2**(수제, 1500ms): 로컬 핸디캡 1 47판 중 16승 10패 21무, 클라우드 핸디캡 2 64판 중 19승 16패 29무(약 39%). 종합 결정 61판 중 약 57%로 깊이 3이 앞서지만 확정 아님.
- **핸디캡과 무승부율**: 핸디캡 0 약 57%, 1 약 41~45%, 2 약 39% -> 말 수 차이가 무승부의 주된 원인이 아님, 반복/50수 규칙 점검 필요.
- **전술 세트**(`nnue/tactics-set.json`, 75문제, 기준 깊이 3 이상): 수제 16.0%, Squall@atanh400 16.0%, blend0.8 17.3%, 잔차 hybrid150 17.3%, hybrid300 18.7% (오차 +-4p) -> 모델을 못 가림.
- **패럿 재검증**: 수 목록 대조 300개 중 0건 차이, 실제 진행 60판 중 5판 갈림(locustSwarm 2건 예상된 차이, promotionRush 1건, 브루터스 1건, 평범한 수 1건) -> 패럿과 무관, 나머지 원인은 미확인.
- **13만 국면 잔차 모델 대전**(수제 상대): 계수 150 45.5%(35/77), 300 46.6%(34/73), 600 55.6%(40/72), 전부 오차 범위.
- **3라운드 전체(150,415) 학습 모델**(핸디캡 1, 12쌍 x 4샤드): 위 6번 표 참조. `blenddepth-r3f`(깊이별 가중, `atanh400`)만 첫 실험에서 win(66.0%)이었으나 독립 재확인 두 번(50.8%, 53.7%)에서 재현되지 않아 unproven으로 정정. 나머지 변형(잔차+유닛 8, 검색 점수 0.8, 깊이별 가중+유닛 8)도 unproven.
